# 事件拆分

将包含多个事件的文本拆分为独立的原子化事件，每个事件保留为一段完整文字并自动补全全局时间/地点。

## 文件说明

| 文件 | 说明 |
|------|------|
| event_splitter.py | EventSplitter 类：规则预处理（按空行/编号粗拆）→ LLM 智能拆分 → 结果验证 |
| model_comparison.py | 模型对比测试脚本，测试不同模型在事件拆分任务上的精度和耗时差异 |

## 使用方式

```bash
python event_splitter.py <input.txt> [output.json]
```
